{
  "secret": {
    "secretType": "ENCRYPTED_TEXT",
    "encryptedText": {
      "secretManagerId": "{{secret_manager_id}}",
      "name": "{{secret_name}}",
      "value": "{{secret_value}}",
      "usageScope": {
        "appEnvScopes": {
          "application": {
            "filterType": "ALL"
          },
          "environment": {
            "filterType": "PRODUCTION_ENVIRONMENTS"
          }
        }
      }
    }
  }
}
